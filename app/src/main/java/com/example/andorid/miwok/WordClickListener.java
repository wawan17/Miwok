package com.example.andorid.miwok;


import android.media.MediaPlayer;
import android.view.View;
import android.widget.Toast;


public class WordClickListener implements View.OnClickListener {

    @Override
    public void onClick(View view) {
        //Toast.makeText(view.getContext(), "Open list number", Toast.LENGTH_SHORT).show();
        //MediaPlayer mediaPlayer = MediaPlayer.create(view.getContext(), R.raw.number_one);
        //mediaPlayer.start();
    }

}
